#include "ui_pick_keypad_input.h"

/*
 * Intentionally empty.
 * The keypad helpers are implemented as inline functions in the header so
 * they can be used without forcing a full CMake regenerate.
 */
